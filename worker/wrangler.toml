# BTC ALT REGIME TRADER — 단계별 복구 진행 중
# 완료: /health, /upbit, /market · 진행 중: PIN 인증 · 예정: KV 포지션 → Telegram → Cron
name = "btc-alt-scalper-v7"
main = "worker.js"
compatibility_date = "2026-09-16"

[[kv_namespaces]]
binding = "SCALPER_KV"
id = "b1d33dfeca5440dbba9b76b769c6bf95"

# PIN — 이번 단계부터 /pin-check에서 사용 (Cloudflare Secret 이름 그대로 PIN)
# 아래는 아직 이 코드에서 쓰지 않는 Secret들 (다음 단계에서 재사용 예정)
# SCALPER_PIN
# TELEGRAM_BOT_TOKEN
# TELEGRAM_CHAT_ID
